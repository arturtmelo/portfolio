// show the path that was asked for, as a terminal would echo it
var p = location.pathname + location.search;
document.getElementById('path').textContent = p;
document.getElementById('path2').textContent = p;
